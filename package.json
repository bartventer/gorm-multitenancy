{
  "engines": {
    "node": "22.4.0"
  },
  "dependencies": {
    "@semantic-release/git": "^10.0.1",
    "semantic-release": "24.0.0"
  },
  "scripts": {
    "release": "cd $GOMODDIR && semantic-release"
  }
}